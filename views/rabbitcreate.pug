extends layout 
 
block content 
  h1= title 
 
  form(id='rabbitForm', action="/resource/rabbit", method="POST") 
    div.form-group 
        label.rabbitAttr(for='rabbit_type') rabbit type 
        input#rabbit_type(type='text', name='type') 
    div.form-group 
        label.rabbitAttr(for='area') rabbit area
        input#area(type='text', name='area') 
    div.form-group 
        label.rabbitAttr(for='cost') rabbit cost 
        input#cost(type='number', name='cost') 
    input(type="submit") 
 
  script. 
    document.forms['rabbitForm'].addEventListener('submit', (event) => { 
      event.preventDefault(); 
      // TODO do something here to show user trabbit form is being submitted 
      fetch(event.target.action, { 
        method: 'POST', 
        body: new URLSearchParams(new FormData(event.target)) // event.target is the form 
      }).then((resp) => { 
        return resp.json(); // or resp.text() or wrabbitever the server sends 
      }).then((body) => { 
        alert("Save succeded") 
        // TODO handle body 
      }).catch((error) => { 
        // TODO handle error 
        alert("Save failed " + error ) 
      }); 
    }); 